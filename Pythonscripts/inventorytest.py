inventory = {'rope': 1, 'torch': 4, 'gold coin': 9, 'dagger': 2, 'arrow': 12}

def displayInventory(item,value):
    inv=0
    for i,n in a:
        number = number + n.get(value, 0)
        return inv

displayInventory(inventory)
