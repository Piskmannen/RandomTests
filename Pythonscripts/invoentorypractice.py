inventory = {'rope': 1, 'torch': 4, 'gold coin': 9, 'dagger': 2, 'arrow': 12}

def displayInventory(item, number):
    print (item)
    #inv = 0
    #for k,v in inventory():
        #print (k)+(' : ')+(v)
       # inv = inv + v.get(item, 0)
    #return inv

#print (str(displayInventory))
#a = inventory.keys
#v = inventory.values
#displayInventory(a,v)

displayInventory()
